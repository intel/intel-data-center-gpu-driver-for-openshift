name: Hadolint and build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      
      - name: Hadolint check on the dockerfile
        uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: docker/intel-dgpu-driver.Dockerfile
          failure-threshold: error

      # - name: Log in to Red Hat Registry
      #   uses: redhat-actions/podman-login@v1
      #   with:
      #     registry: registry.redhat.io
      #     username: ${{ secrets.RH_USERNAME }}
      #     password: ${{ secrets.RH_ACCESS_TOKEN }}

      # - name: Log in to Quay.io Registry
      #   uses: redhat-actions/podman-login@v1
      #   with:
      #     registry: quay.io
      #     username: ${{ secrets.QUAY_USERNAME }}
      #     password: ${{ secrets.QUAY_PASSWORD }}

      # - name: Set up Docker Buildx
      #   uses: docker/setup-buildx-action@v2

      # - name: Build driver container
      #   uses: docker/build-push-action@v4
      #   with:
      #     context: ./
      #     file: docker/intel-dgpu-driver.Dockerfile
      #     push: false